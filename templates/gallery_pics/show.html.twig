{% extends 'base.html.twig' %}

{% block title %}GalleryPics{% endblock %}

{% block body %}
    <h1>GalleryPics</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gallery_pic.id }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ gallery_pic.category }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ gallery_pic.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gallery_pics_index') }}">back to list</a>

    <a href="{{ path('app_gallery_pics_edit', {'id': gallery_pic.id}) }}">edit</a>

    {{ include('gallery_pics/_delete_form.html.twig') }}
{% endblock %}
